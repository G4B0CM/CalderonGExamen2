<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CalderonGExamen2.Gridy"
             Title="Gridy">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <BoxView />
        <Label Text="Gabriel"
               TextColor="White"
               HorizontalOptions="Center"
               VerticalOptions="Center" />
        <BoxView Grid.Column="1"
                  />
        <Label Grid.Column="1"
               Text="Alonso"
               TextColor="White"
               HorizontalOptions="Center"
               VerticalOptions="Center" />
        <BoxView Grid.Row="1"
                 />
        <Label Grid.Row="1"
               Text="Calderon"
               TextColor="White"
               HorizontalOptions="Center"
               VerticalOptions="Center" />
        <BoxView Grid.Row="1"
                 Grid.Column="1"
                />
        <Label Grid.Row="1"
               Grid.Column="1"
               Text="Matute"
               TextColor="White"
               HorizontalOptions="Center"
               VerticalOptions="Center" />
    </Grid>
</ContentPage>